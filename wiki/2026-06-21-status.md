# Project Status — 2026-06-21

> **UPDATE (2026-06-21, GF frame correction).** After this snapshot the GuessFactor
> pipeline was corrected (gun **pre-aims** for the fire tick; VCS **trains in the aim-time
> frame**; producer `our_fire_lateral_velocity`/`_advancing_velocity`/`_direction` were
> sign-aligned to the core/`scan_*` convention) and the gun was **retrained from scratch**
> in the new frame (3 generations, full 54-opponent roster, aggregate hit rate **~13.8 %**,
> converged — more generations gave no further gain). All gun/GF numbers below are
> **regenerated** from `pipeline/build/intuition-run/1782077581678-…`; the pre-correction
> corpora (seed 12345, the 50-round 6.7 GB run, the old `vcs.dat`) are **superseded**. The
> biggest conclusion change: the old **gun-side lag-1 ≈ 0.69** signal was a direction-less
> artifact — on the corrected frame the gun GF is **not** lag-1 autocorrelated, and the
> later re-measure (aim-time-direction fix applied to *incoming* waves too) shows the
> **movement** channel collapses the same way — neither channel carries exploitable lag-1
> dodge memory (see below / [ML-revelations.md](ML-revelations.md)).

> Baseline current-state assessment for `robocode-autopilot`. **No planning here** —
> this is a grounded snapshot to plan *from*. Each claim is tied to code or to a
> generated artifact. Quantitative backbone is the fresh 10-round run
> [2026-06-21-ML-intuition.md](2026-06-21-ML-intuition.md) (seed 12345; 25-matchup,
> 50-perspective, 1.26 GB), cross-checked against the larger 50-round baseline
> [ML-intuition.md](ML-intuition.md) (~6.7 GB) and
> [build/battle-summary.txt](../build/battle-summary.txt).

## TL;DR — assessment verdict

| Your claim | Verdict | Grounding |
| --- | --- | --- |
| Reliable pipeline for offline learning | **Confirmed** | Layer 0 fidelity = 0 mismatches over millions of checks; large checksummed CSV corpus |
| Robot reliably scans + detects opponent fire online | **Confirmed (with nuance)** | Scan coverage 96–99%; fire detection ~95–100% vs clean shooters; the noisy count-ratio metric is confounded by energy bookkeeping, not blind detection |
| Decent VCS gun learning online | **Partially confirmed** | VCS learns + persists online, but fresh data puts hit rate at/below the head-on baseline — not beating zero-learning |

Bottom line: the **measurement/data foundation is the strong, trustworthy part**.
The **gun's competitive edge is the weak part** — it works mechanically but has
not yet been shown to beat a zero-learning baseline.

---

## 1. Offline learning pipeline — CONFIRMED RELIABLE

**Architecture** ([replay-architecture.md](../replay-architecture.md)): a deterministic
observer shadow re-runs `Autopilot` from reconstructed partial-information events;
its whiteboard is the data source. A separate god-view whiteboard measures perception
quality but never mutates robot-side state (the two-whiteboard rule).

**Fidelity is proven, not assumed:**
- **Layer 0 (IDebugProperty fidelity)** — in-game robot vs observer shadow: **0
  mismatches** at `checks=745534` (Aggressive) and `checks=3315293` (Fire) in
  [battle-summary.txt](../build/battle-summary.txt). This is the load-bearing
  guarantee: the observer got the robot's *exact* experience.
- **Layer 1 (spatial/kinematic)** — **0 spatial mismatches**.
- **Layer 4 (energy accounting)** — 2751/2751 and 5646/5646 checks **100%**.

**Corpus exists and is integrity-checked:** a fresh 10-round producer run (seed 12345,
[2026-06-21-ML-intuition.md](2026-06-21-ML-intuition.md)) inventories 25 matchups ·
5 robots · 50 perspectives · **1.26 GB**, every CSV present with SHA-256 + row counts;
label budget **186,597** real dejavu waves (targeting), **171,257** incoming waves
(movement). The larger 50-round baseline ([ML-intuition.md](ML-intuition.md), ~6.7 GB,
~1.03 M / 586 K labels) corroborates every headline number below. (Note: Layer 0
fidelity is validated by `BattleLoopTest`, not the producer — the fidelity evidence
above remains from [battle-summary.txt](../build/battle-summary.txt).)

**Caveats (grounded):**
- `test.SittingDuck` leg in [battle-summary.txt](../build/battle-summary.txt) shows
  an `AssertionFailedError` (BattleLoopTest.java:177) with incoming-fire detection
  0.0% — expected for a non-firing bot, but it means that assertion is not
  opponent-aware and the summary is not currently all-green.
- Several Layer 1–4 fields render empty in the summary text (formatting/printf gaps),
  so the human-readable rollup is less trustworthy than the underlying checks.
- Pipeline scope is **1v1 only, no `SkippedTurnEvent`** (by design).

## 2. Online scan + fire detection — CONFIRMED (with nuance)

**Radar/scan:** `NarrowLockRadar` keeps a tight 1v1 lock. Scan coverage is
96.3–99.5% with median ticks-since-scan = 1 across all five reference opponents
(fresh §B6). Solid.

**Incoming-fire detection** ([TheirWaveTracker](../core/src/main/java/cz/zamboch/autopilot/core/features/TheirWaveTracker.java)):
scan-to-scan opponent energy drop, adjusted for wall-hit damage and inactivity-zap,
classified into fire power [0.1, 3.0]. The reported "detection rate" is
`robotSideFires / godViewFires` — a **count ratio, not a fraction of true fires**
([GodViewQualityValidator](../pipeline/src/main/java/cz/zamboch/autopilot/pipeline/GodViewQualityValidator.java)),
and its god-view denominator can itself miss fires. Realized values
([battle-summary.txt](../build/battle-summary.txt)): **Fire 95.7%, BeepBoop 100%,
Aggressive 90.3%, Walls 136.8%, Crazy 156.6%**.

The **>100% values prove the metric is not a blind-detector accuracy**: against clean
shooters detection is ~**95–100%**; the deviations (both below and above 100%) are
dominated by **energy-bookkeeping confounds** — ram/wall damage overlapping the fire
drop (Aggressive), the inactivity-zap leak inflating counts (Walls/Crazy), and god-view
resolver tick-aliasing (BeepBoop, since fixed). The radar/scan layer reliably
*perceives* shots; the residual error is in disambiguating energy drops. My earlier
"~90%" framing understated this.

**Caveats:**
- Confound-driven mis-attribution still propagates to movement that consumes these
  waves; the error is worst on ram-heavy / erratic-energy bots.
- `their-waves.csv` has ~0.01% unresolved/NaN break rows (round-end censoring, §A4) —
  not missing at random.

## 3. VCS gun, online learning — PARTIALLY CONFIRMED

**Mechanically working:**
- [VcsStore](../core/src/main/java/cz/zamboch/autopilot/core/VcsStore.java) is a
  compact `int[5][5][3][31]` (distance × lateral-velocity × lag-1 dodge-context × GF)
  = 2325 bins, with lag-1 → aggregate fallback for cold start.
- Learns **online**: resolved real waves increment bins; persisted to
  `robot/data/vcs.dat` keyed by opponent FNV-1a hash, accumulating across rounds and
  battles ([Autopilot.java](../robot/src/main/java/cz/zamboch/Autopilot.java)).
  Observer instances load but **never persist** (correct separation).
- [ModelSelector](../core/src/main/java/cz/zamboch/autopilot/core/ModelSelector.java)
  provides a regret-windowed multi-model competition framework — but only `VcsStore`
  is currently wired, so it is a no-op selector today.

**The competitiveness gap (regenerated, corrected frame §C2/§C4/§C5):**
- Floor (current-Autopilot reconstruction) gun hit rate: **9.4–10.4 %** depending on opponent.
- Head-on (zero-learning) baseline: **9.5–10.9 %**.
- Best-static ceiling: **10.6–12.2 %** (CV-model **11.0–12.9 %**).
- In-game hero hit rate (retrained gun): **7.6–10.3 %** (fired at self-selected ticks —
  unpaired, trap #7, so not directly comparable).

→ On the corrected frame the VCS gun is still **at or below the head-on baseline for most
opponents** (e.g. DrussGT 9.18 % hero vs 10.89 % head-on; Knight 10.27 % vs 10.82 %). GF
distributions are flat (entropy ≈ 5.34–5.41 bits; top-1 bin mass 4.3–6.5 %), so these
opponents are genuinely hard to hit, but the best-static ceiling (~10.6–12.2 %) shows real
headroom the current gun is not capturing. (Across the **full 54-opponent** training roster,
including easy sample bots, the retrained aggregate is ~13.8 %; the later box-convolution
aim + distance-aware (precise) MEA re-measure lifted the latest generation to **15.1 %**.)

**Other grounded facts for planning context (regenerated):**
- Strongest GF segmentation axis is **lateral velocity** (η² **0.0755**, the highest
  mutual-information feature at 0.103 nats); advancing velocity adds less (η² 0.0416) and
  distance is negligible (η² 0.0004) (§C6/§C7). Sign-agnostic, so this survives the frame fix.
- **CORRECTED (re-measured):** GF lag-1 autocorrelation is **≈ 0 on BOTH channels** on the
  directioned aim-time frame — gun (`our_break_gf`) −0.01…0.11 and, once the same
  aim-time-direction fix is applied to incoming waves, movement (`their_break_gf`) 0.05…0.07
  with leakage-safe `developing_gf`→GF ≈ 0. The old ≈0.69 (and the earlier "it lives on the
  movement channel" reading) was the direction-less framing artifact on both channels.
  Neither a sequence/pattern **gun** nor a movement sequence model beats the geometric VCS
  segmentation here; the levers are the geometric axes (lateral/advancing velocity), not
  lag-1 dodge memory (§C8 / [sequence.md](sequence.md)).
- Most opponents are stationary after bias correction; `jk.mega.DrussGT` shows the
  only non-zero adaptation (KL 0.104, CI excludes 0) — weaker than the 50-round
  baseline (0.377) because fewer rounds reveal less drift → recency-weighting still
  matters there (§F2).
- Opponent strength (§G2, reference-bot round-robin — **Autopilot excluded by design**):
  `kc.mega.BeepBoop` is strongest (0 losses to peers), `aaa.r.ScalarR` weakest (2/6),
  the rest mid. This ranks targeting difficulty and is **not** Autopilot's own record
  (Autopilot is 0/10 vs BeepBoop per battle-summary.txt).

## 4. Movement — context (not in your claim, but part of baseline)

Baseline `OrbitMovementStrategy` only; no wave surfing yet. Defensive label data
exists (`their-waves.csv`, `their_break_gf` / `their_hit_us`) and dodge profiles are
flat/high-entropy (§D1), i.e. movement is already hard to hit but is **not
model-driven**. Fresh hit-us rate 8.0–9.8%, worst vs `kc.mega.BeepBoop` (9.79%, rising
to 14.1% under high-power ≥1.5 bullets — §D2).

## 5. Component reliability matrix

| Component | State | Evidence |
| --- | --- | --- |
| Replay/observer pipeline | Reliable | Layer 0 = 0 mismatches |
| CSV corpus + integrity | Reliable | 50/50 perspectives, SHA-256 checksummed |
| Spatial/energy reconstruction | Reliable | Layer 1 = 0, energy 100% |
| Online scan/radar lock | Reliable | 96–99% coverage |
| Incoming-fire detection | Reliable vs clean shooters | ~95–100%; confounded count-ratio metric, not blind detection |
| VCS gun learning loop | Works | online increment + per-opponent persist |
| VCS gun competitiveness | Unproven | ≈ head-on baseline, ceiling unreached |
| Movement intelligence | Baseline only | orbit, no surfing |
| ModelSelector multi-model | Scaffolding | only VCS wired |

## 6. Known traps carried into any future work

From [ML-intuition.md](ML-intuition.md) §I4: autopilot fan is a counterfactual (use
dejavu real waves for truth); virtual-wave leakage (group by parent fire); variable
per-wave label latency (~28 ticks, scales with distance/power); round-end censoring;
self-play excluded from aggregates; floor↔hero comparison is unpaired.

---

*Sources: [replay-architecture.md](../replay-architecture.md),
[IDebugProperties-intent.md](../IDebugProperties-intent.md),
[GodView-intent.md](../GodView-intent.md), fresh
[2026-06-21-ML-intuition.md](2026-06-21-ML-intuition.md) (seed 12345, 10-round) +
baseline [ML-intuition.md](ML-intuition.md),
[build/battle-summary.txt](../build/battle-summary.txt), and the `core/`, `robot/`,
`pipeline/` sources cited inline.*
